pipeline {
    agent {node { label 'slave' }}
    stages{
        stage("one") {
            echo "One"
        }
        stage("two") {
            echo "two"
        }
        stage("three") {
            echo "three"
        }
    }
}
